@model IEnumerable<GravarRecuperarImagens.Models.Imagem>

@{
    ViewBag.Title = "Listar Imagens";
}

<h2>Listar Imagens</h2>
<p>
    <a class="btn btn-xs btn-info" href="@Url.Action("ImportarImagens")">Importar Imagens</a>
</p>
<table class="table">
    <tr>
        <th></th>
    </tr>
    @{
        if (Model != null)
        {
            <tr>
                <th>
                    ID
                </th>
                <th>
                    BLOB
                </th>
                <th>
                    AÇÕES
                </th>
            </tr>
            foreach (var item in Model)
            {
                <tr>
                    <td>teste</td>
                    <td><img src="data:image;base64,@Convert.ToBase64String(item.Img)" width="300" /></td>

                    <td></td>
                </tr>
            }
        }

    }

</table>
